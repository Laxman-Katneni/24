// This file is no longer needed - removed to fix cookie domain validation error
// Render's infrastructure rejects custom cookie domains like .onrender.com
// Instead, we use the frontend proxy approach where all requests go through the same domain

